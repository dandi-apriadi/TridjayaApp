*.iml
.gradle/
.kotlin/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Gradle build outputs (all modules)
build/
app/build/
baselineprofile/build/

# Release signing — never commit the keystore or its credentials.
keystore.properties
*.jks
*.keystore

# Firebase config (contains project keys; app builds without it — add per-machine).
# Remove this line if you intend to commit google-services.json to the repo.
app/google-services.json

# Local dev artifacts
serve.log
*.log

# APK rilis: artefak build, bukan sumber. Bisa dibangun ulang dari tag/commit,
# dan yang beredar tersimpan di VPS (services/user-service/uploads/apk).
# Menyimpan biner ~5 MB per rilis di git menempel di riwayat selamanya.
releases/
.hermes.md
